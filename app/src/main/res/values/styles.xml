<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>


    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="repo_item_cardView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginBottom">@dimen/space_all_small</item>
        <item name="android:layout_marginStart">@dimen/space_all_medium</item>
        <item name="android:layout_marginEnd">@dimen/space_all_medium</item>
        <item name="android:clickable">true</item>
        <item name="contentPadding">@dimen/space_all_small</item>
        <item name="cardElevation">@dimen/shadowSize_main_recycler_cardView</item>
        <item name="cardMaxElevation">@dimen/shadowSize_main_recycler_cardView</item>
        <item name="cardCornerRadius">@dimen/rad_main_recycler_cardView</item>
        <item name="android:foreground">?android:attr/selectableItemBackground</item>
    </style>

    <style name="repo_first_item_cardView" parent="repo_item_cardView">
        <item name="android:layout_marginTop">@dimen/space_all_small</item>
    </style>

    <style name="repo_item_details">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">@dimen/textSize_all_medium</item>
        <item name="android:ellipsize">end</item>
        <item name="android:singleLine">true</item>
    </style>

    <style name="repo_item_repoName" parent="repo_item_details">
        <item name="android:textSize">@dimen/textSize_all_large</item>

    </style>

</resources>
